var typed = new Typed(".feature-text", {
  // Waits 1000ms after typing "First"
  strings: ["society", "environment", "India"],
  typeSpeed: 20,
  backSpeed: 20,
  backDelay: 2000,
  loop: true,
  autoInsertCss: true
});


$('.back-image').maximage({
  cycleOptions: {
    timeout: 2000,
    speed: 500
  }
});
